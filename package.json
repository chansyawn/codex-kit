{
  "name": "codex-kit",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "postinstall": "vp config",
    "check": "vp check",
    "test": "vp run -r test",
    "build": "vp run -r build",
    "ready": "vp run check && vp run test && vp run build",
    "dev": "vp dev apps/runtime",
    "cli": "vp run @codexkit/cli#cli"
  },
  "devDependencies": {
    "vite-plus": "catalog:"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "11.9.0",
      "onFail": "download"
    }
  },
  "engines": {
    "node": ">=22.18.0"
  }
}
